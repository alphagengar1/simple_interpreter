# simple_interpreter

A small language interpreter written from scratch in C++

Right now it evaluates arithmetic (precedence, associativity, parens) on integers

Plans -

- variables and assignment (in progress)
- comparisons + if/while, maybe running fizzbuzz in it
- functions and recursion
- a bytecode compiler + stack VM, benchmarked against the tree-walker
- running the whole thing on a rasberry
